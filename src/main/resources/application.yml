server:
  port: 8082

spring:
  application:
    name: parking-lot-design

  datasource:
    url: jdbc:postgresql://localhost:5434/ParkingLot
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Jjh123123!

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.parking: DEBUG

